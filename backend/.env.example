# Use the same long random value on the VPS backend and restaurant desktop print agent.
# Required for durable KOT catch-up/acknowledgement when Socket.IO disconnects.
PRINT_AGENT_KEY=replace-with-a-long-random-secret
